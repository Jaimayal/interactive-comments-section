---
const { image, username, createdAt } = Astro.props;
---
<header class="flex flex-row items-center justify-start">
    <img class="w-10 h-10 mr-3 rounded-full" src={image} />
    <h4 class="mr-3 font-bold text-black">{username}</h4>
    <p class="text-gray-500">{createdAt}</p>
</header>